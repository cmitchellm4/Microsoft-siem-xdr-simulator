async def init_db():
    """Initialize database — placeholder until PostgreSQL is configured."""
    print("✅ Database ready (running without PostgreSQL for now)")

async def get_db():
    """Placeholder database session dependency."""
    yield None